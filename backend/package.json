{
  "name": "@gitledger/backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "check": "tsc --noEmit",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "db:migrate:sql": "bun src/scripts/migrate.ts"
  },
  "dependencies": {
    "@trpc/server": "^11.0.0",
    "drizzle-orm": "^0.36.0",
    "hono": "^4.6.0",
    "zod": "^3.23.8",
    "pg": "^8.13.1",
    "redis": "^4.7.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.27.0",
    "typescript": "^5.6.3",
    "@types/pg": "^8.11.10"
  }
}
