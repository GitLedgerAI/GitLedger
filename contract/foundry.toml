[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.24"
test = "test"
script = "script"
optimizer = true
optimizer_runs = 200

[rpc_endpoints]
base = "${BASE_RPC_URL}"
base_sepolia = "${BASE_SEPOLIA_RPC}"
